quarkus:
  datasource:
    db-kind: postgresql
    jdbc:
      url: jdbc:postgresql://tfb-database:5432/hello_world
"%dev":
  quarkus:
    datasource:
      jdbc:
        url: jdbc:postgresql://localhost:5432/hello_world
